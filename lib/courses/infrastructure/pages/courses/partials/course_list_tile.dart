import 'package:flutter/widgets.dart';

class CourseListTileWidget extends StatelessWidget {
  const CourseListTileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
